<div <?= $field->hasAlign($data) ? "class=\"text-{$field->getAlign($data)}\"" : '' ?>>
    <div class="<?= $field->hasType($data) ? ' spinner-' . $field->getType($data) : '' ?><?= $field->hasStyle($data) ? ' text-' . $field->getStyle($data) : '' ?>" role="status">
        <span class="sr-only"><?= $this->getValue($data) ?>></span>
    </div>
<div>
