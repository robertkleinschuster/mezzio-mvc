<section>

    <table class="table table-bordered">
        <thead>
        <tr>
            <?php foreach ($fields as $key => $field): ?>
                <td><?= $field->getName() ?></td>
            <?php endforeach ?>
        </tr>
        </thead>
        <tbody>
        <?php foreach ($model->getOverview() as $row): ?>
            <tr>
                <?php foreach ($fields as $field): ?>
                    <td><?php $this->insert($templateFolder . '::' . $field->getTemplate(), ['templateFolder' => $templateFolder, 'field' => $field, 'value' => $row[$field->getKey()]]) ?></td>
                <?php endforeach ?>
            </tr>
        <?php endforeach ?>
        </tbody>
    </table>

</section>
